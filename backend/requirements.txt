# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# LangChain - using flexible versions to avoid conflicts
langchain>=0.1.0
langchain-openai>=0.0.2
langchain-anthropic>=0.1.0
langchain-google-genai>=0.0.6
langchain-community>=0.0.20

# Vector database
chromadb==0.4.22

# LLM providers (install what you need)
openai>=1.6.1
anthropic>=0.18.1
google-generativeai>=0.3.2

# Hugging Face (optional - only if using Hugging Face)
# langchain-huggingface>=0.0.1
# huggingface-hub>=0.23.0

# Document processing
pypdf2==3.0.1
python-docx==1.1.0

# Image processing
Pillow>=10.0.0
pytesseract>=0.3.10

# Audio/Video processing
moviepy>=1.0.3  # Video processing and audio extraction (requires FFmpeg)
opencv-python-headless>=4.8.0  # Video frame extraction for server deployments
openai-whisper>=20231117  # Audio transcription (requires FFmpeg installed on system)
pydub>=0.25.1  # Audio manipulation
ffmpeg-python>=0.2.0  # Python wrapper for FFmpeg (FFmpeg binary must be installed separately)
imageio-ffmpeg>=0.4.0  # FFmpeg wrapper for imageio (dependency of moviepy)
# Whisper dependencies (installed automatically with openai-whisper, but listed for clarity)
torch>=2.0.0  # Deep learning framework (required by Whisper)
numba>=0.56.0  # JIT compiler (required by Whisper)
tiktoken>=0.5.0  # Tokenizer (required by Whisper)
imageio-ffmpeg>=0.4.0  # FFmpeg wrapper for imageio (used by moviepy)

# WebSocket support
websockets>=12.0
python-socketio>=5.10.0

# Embeddings (free alternative)
# Using compatible versions to avoid huggingface_hub conflicts
sentence-transformers>=2.2.0,<3.0.0
huggingface-hub<0.20.0
numpy<2.0.0,>=1.22.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.2
pydantic-settings==2.1.0
httpx==0.25.2
email-validator>=2.0.0

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
